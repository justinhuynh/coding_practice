require 'spec_helper'

describe CodingPractice do
  it 'has a version number' do
    expect(CodingPractice::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
